<launch>

  <!-- <rosparam command="load" file="$(find robot_self_filter)/config/self_filter_param.yaml" /> -->
  <arg name="calibrate" default="0" />

  <node unless="$(arg calibrate)" pkg="robot_self_filter" name="self_filter" type="self_filter" output="screen" respawn="true"/>

</launch>
